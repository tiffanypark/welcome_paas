--

image_resource:
  type: docker-image
  source:
    repository: alpine
    tag: latest

platform: linux

inputs:
  - name: sysmaster-web-git
  
run:
  path:  /bin/chmod
  args: [777, ./sysmaster-web-git/mvnw]
  
